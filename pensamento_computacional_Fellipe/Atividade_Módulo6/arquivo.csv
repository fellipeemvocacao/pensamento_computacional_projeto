ARQUIVO_CSV = "notas_alunos.csv"
CAMPOS = ["Nome", "Matéria", "Nota"]

def adicionar_nota(nome, materia, nota):
    """Adiciona uma nova linha ao arquivo CSV sem apagar os dados existentes."""
    arquivo_existe = os.path.exists(ARQUIVO_CSV)
    
    with open(ARQUIVO_CSV, "a", newline="", encoding="utf-8") as arquivo:
        escritor = csv.DictWriter(arquivo, fieldnames=CAMPOS)
        
        if not arquivo_existe:
            escritor.writeheader()
            
        escritor.writerow({"Nome": nome, "Matéria": materia, "Nota": nota})
    print(f"✔️ Nota de {nome} salva com sucesso!")

def carregar_e_exibir():
    """Lê o arquivo CSV e exibe os dados formatados."""
    if not os.path.exists(ARQUIVO_CSV):
        print("⚠️ O arquivo ainda não contém registros.")
        return

    print(f"\n{'NOME':<15} | {'MATÉRIA':<15} | {'NOTA':<5}")
    print("-" * 45)
    
    with open(ARQUIVO_CSV, "r", encoding="utf-8") as arquivo:
        leitor = csv.DictReader(arquivo)
        for linha in leitor:
            print(f"{linha['Nome']:<15} | {linha['Matéria']:<15} | {linha['Nota']:<5}")

adicionar_nota("Alice", "Matemática", 8.5)
adicionar_nota("Bruno", "Física", 7.0)
adicionar_nota("Carla", "História", 9.2)

carregar_e_exibir()